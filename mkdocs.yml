site_name: TaskFlow Internal Developer Portal
site_description: Internal Developer Portal for the TaskFlow platform

repo_url: https://github.com/OnyiGlobal2025/taskflow-backstage
edit_uri: edit/main/docs/

nav:
  - Home: index.md
  - Architecture: architecture.md

plugins:
  - techdocs-core

# Note: no pymdownx.superfences custom_fences block.
# The `!!python/name:` YAML tag is rejected by techdocs-cli's strict loader.
# Mermaid is rendered client-side by the TechDocs addon in the Backstage
# frontend, so plain ```mermaid fenced blocks work without MkDocs config.